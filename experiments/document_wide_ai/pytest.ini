[pytest]
# Run as: python -m pytest experiments/document_wide_ai -c experiments/document_wide_ai/pytest.ini
# from the repo root, so `experiments.document_wide_ai...` imports resolve.
testpaths = tests
addopts = -q
