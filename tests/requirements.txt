# Test-only dependencies. NOT shipped — api/requirements.txt is the runtime set, and
# nothing under api/ imports these (verified: only scripts/ and tests/ do).
#
# The OOXML authoring libraries build fixtures on the fly (scripts/gen_demo_fixtures.py
# and ~12 test modules construct .docx/.pptx/.xlsx inputs rather than committing
# binaries). They were previously undeclared and only ever present ambiently on dev
# machines: CI installed api/requirements.txt alone and never noticed, because every
# test that needs them is gated on the Office engine and the engine wasn't built on CI.
# Building the engine there (see azure-pipelines.yml) makes them load-bearing.
python-docx
python-pptx
openpyxl
pytest
pytest-xdist
# Splits the suite across CI runners (matrix) — each shard runs `--splits N --group i`.
# Composes with -n auto/--dist loadfile: pytest-split selects the shard's subset, xdist
# parallelises within it. Free-runner equivalent of a larger runner.
pytest-split
# Reads .github/workflows/*.yml in the workflow guards. It was previously UNDECLARED while
# test_ci_alert_workflow.py gated itself on `pytest.importorskip("yaml")` — so on CI, which
# installs exactly this file and api/requirements.txt, that module skipped on every run and had
# never once executed. What it guards is ci-alert.yml, the thing that announces CI going red on
# main; the 2026-08-20 outage in its docstring is an hour of silently skipped deploys. The guard
# against that recurring was itself silent. See tests/test_undeclared_importorskip.py, which now
# fails on any importorskip naming a dependency declared nowhere CI installs.
PyYAML

# The PDF/UA-1 renderer behind api/report_weasy.py and the ACR accessible export. DECLARED here
# rather than left ambient because tests/test_report_weasy_structure.py and
# tests/test_report_pdfua_gap.py both `importorskip("weasyprint")`, and an undeclared
# importorskip is the silent-skip shape tests/test_undeclared_importorskip.py exists to refuse:
# CI installs exactly this file and api/requirements.txt, so without this line those 21 tests
# would skip on every run and the PDF/UA gate would be a claim nothing evaluated.
#
# veraPDF is the OTHER half of that gate and cannot be declared here — it is a Java tool, not a
# wheel. It is installed by its own step in ci.yml; tests/verapdf.py degrades to a stated skip
# when it is absent, which is why the step exists rather than relying on the skip.
weasyprint
