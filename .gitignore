# secrets / env
.env
*.local
# python
__pycache__/
*.pyc
.venv/
.venv-drive/
.mypy_cache/
.ruff_cache/
.pytest_cache/
# .net
bin/
obj/
# node / ui
node_modules/
dist/
# os
.DS_Store

# base64 upload sidecars
test-corpus/files/*.b64

# service-account keys / secrets
.secrets/
*-sa.json

# local sqlite
acp.db

# runtime-edited rubric
config/rubric.active.json

# generated scan artifacts
test-corpus/last-scan.json
test-corpus/files/_office.json

# vendored compiled engine for the deploy image (not committed)
deploy/public/vendor/

# Partner code (DigitalA11y) — lives in its own repo: github.com/aveeranjan/digital-accessibility
# Consumed as a pinned build artifact, not vendored here.
digital-accessibility/
